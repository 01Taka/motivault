import { techniquePathBase } from '../../../constants/path/path-constants'

const taskPressBasePath = `${techniquePathBase}/task-press`
const createTaskPressPath = `${taskPressBasePath}/create`

export { taskPressBasePath, createTaskPressPath }
