import { MINUTES_IN_MS } from '../../../constants/datetime-constants'

export const STUDY_TIME_MS_PER_EXP_RATIO = 10 / MINUTES_IN_MS

export const POMODORO_TIMER_AUTO_STOP_TIME_MS = 90 * MINUTES_IN_MS
