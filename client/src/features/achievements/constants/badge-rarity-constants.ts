import type { AchievementBadgeRarity } from '../types/achievement-types'

export const rarityNumber: Record<AchievementBadgeRarity, number> = {
  common: 1,
  uncommon: 2,
  rare: 3,
  epic: 4,
  legendary: 5,
}
