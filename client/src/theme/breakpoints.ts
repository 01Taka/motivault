// src/theme/breakpoints.ts

// ブレイクポイントの設定を定義
const breakpoints = {
  values: {
    xs: 0,
    sm: 600,
    md: 900, // MUIのデフォルトは960だが、900に調整
    lg: 1200,
    xl: 1536,
  },
}

export default breakpoints
